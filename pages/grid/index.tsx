import { Layout } from '~/layouts';

export default function GridIndexPage() {
	return (
		<Layout.Base>
			<div className="flex justify-center items-center min-h-screen px-4 py-2 bg-green-500 default-focus default-transition">
				Grid
			</div>
		</Layout.Base>
	);
}
