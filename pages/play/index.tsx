import { Layout } from '~/layouts';

export default function PlayIndexPage() {
	return (
		<Layout.Base>
			<div className="flex justify-center items-center min-h-screen px-4 py-2 bg-blue-500 default-focus default-transition">
				Play
			</div>
		</Layout.Base>
	);
}
